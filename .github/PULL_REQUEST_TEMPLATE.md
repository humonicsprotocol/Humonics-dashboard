## What does this PR do?

## Checklist
- [ ] `npm run build` passes
- [ ] Loading, error, and empty states handled in all affected components
- [ ] No SDK calls directly in components (hooks only)
- [ ] No private keys in the browser
- [ ] Accessible — labels, aria attributes, role="alert" on errors
